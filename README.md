# Component Management @ Vicara

## Implementation on Windows

### Setup Procedure (Before First Run)

```
$   virtualenv venv
$   venv\Scripts\activate

$   (venv) pip install -r requirements.txt
```

### Run Caya

```
$   venv\Scripts\activate
$   (venv) python main.py
```
The server is available @ localhost:5000
